# PrepEdgeAi

AI-powered interview preparation. Generate company-specific technical interview questions instantly using LLMs.

## Deployment Setup

### Backend (Render)
- Deployed using Docker via `render.yaml` Blueprint.
- Connected to a managed PostgreSQL database.

### Frontend (Vercel)
- React / Vite application.
- Uses `vercel.json` for client-side routing.
